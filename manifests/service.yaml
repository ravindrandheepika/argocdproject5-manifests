apiVersion: v1
kind: Service
metadata:
  name: argocdproject5-service
spec:
  selector:
    app: argocdproject5-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
